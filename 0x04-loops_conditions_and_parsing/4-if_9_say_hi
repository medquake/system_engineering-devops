#!/usr/bin/env bash
# This script loops 10 times, says Hi on the 9th, using a while loop
counter=1
while [ "$counter" -le 10 ]
do
    echo "Best School"
    if [ "$counter" -eq 9 ]
    then
        echo "Hi"
    fi
    ((counter++))
done
